import * as utilities from '../thirdPartyutilities/utilities';

const getUniqueObjects = (array, property) => utilities.uniqObjectsBasedOnAspecificProperty(array, property);

export default getUniqueObjects;